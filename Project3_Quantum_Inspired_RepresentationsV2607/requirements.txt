# Project 3 — Python dependencies (PINNED, BMAD v51 — Appendix K, M4)
# Exact versions verified from the `malaria_md` environment (Aug 1, 2026).
# Install: python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt
#
# These pins reproduce the canonical P3 benchmark results (hybrid / ablation /
# QKS at n=5,000 and n=19,849). PennyLane 0.45.1 is required for the
# state-vector quantum kernel. See the Code Availability statement in
# Paper3_Quantum_Inspired_SM_V2607.tex for the full environment record.

# --- core scientific stack ---
numpy==2.4.6
pandas==2.3.3
scipy==1.17.1
scikit-learn==1.9.0

# --- cheminformatics ---
rdkit==2025.3.6
datamol==0.12.5

# --- persistent homology ---
gudhi

# --- tensor / ML extras ---
matplotlib==3.10.9
umap-learn==0.5.12
joblib==1.5.3
tensorly==0.9.0
ripser==0.6.14

# --- quantum kernel ---
pennylane==0.45.1
pennylane-lightning==0.45.0

# --- differentiable PH (D-GRIL build attempt only; see SM sec. D-GRIL) ---
torch==2.13.0
torch-geometric==2.8.0.post1

# --- misc ---
requests==2.34.2
