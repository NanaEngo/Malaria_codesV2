# Project 3 — Python dependencies
# Python 3.12 (tested with 3.12.3)
# Install: python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt
#
# NOTE: exact versions used in the analysis environment are not pinned here.
# The pins below record the package set required to run the analysis scripts
# in scripts/. Reproducing the exact deposited results additionally requires
# the versions listed in manuscript/LaTeX/Paper3_Quantum_Inspired_SM_V2607.tex
# (PennyLane 0.45.1; see the Code Availability statement for the full record).

# --- core scientific stack ---
numpy
pandas
scipy
scikit-learn

# --- cheminformatics ---
rdkit
datamol

# --- persistent homology ---
gudhi

# --- tensor / ML extras ---
matplotlib
umap-learn

# --- quantum kernel ---
pennylane        # >=0.45.1 (lightning.qubit / lightning.gpu via best_device())
pennylane-lightning

# --- differentiable PH (D-GRIL build attempt only; see SM sec. D-GRIL) ---
torch
torch-geometric

# --- misc ---
requests
