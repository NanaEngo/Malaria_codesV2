# Submission Manifest — P4

**Journal:** Journal of Cheminformatics (Springer)
**Pages:** main 14 p. / SM 4 p. / cover 2 p.
**Status:** PRE-SUBMISSION — compiles clean (0 errors / 0 undefined / 0 overfull / 0 underfull / 0 rerun-requested, main and SM), 20 references
**Generated:** 2026-08-17 21:26 UTC

> **Current as of this build.** Every size, hash and page count below was read from the working tree in
> one pass, after `scripts/p4_replot_publication.py` re-rendered all twelve figure files and `latexmk`
> rebuilt both documents from them (all three commands `exit=0`, stderr empty). File mtimes confirm the
> ordering — figures 22:07:40–41 local, then `refined.pdf` 22:13:25 and `SM.pdf` 22:13:26 — so neither
> PDF predates the figures it embeds. Page counts come from `pdfinfo` on the PDFs listed here and agree
> with the L4 audit note (14 / 4 / 2). The maker≠checker integrity gate has now run in full — all four
> done-criteria closed — and returned a negative verdict; three CRITICAL defects block packaging. See
> the last provenance note.

## File inventory (SHA-256)

| File | Size (B) | SHA-256 |
|---|---:|---|
| P4_Pareto_MCTS_JoC_refined.tex | 52709 | `5254327c58465d476d0dc000c8aeac0411956fb33ec99b4fbaaafc942950e5cd` |
| P4_Pareto_MCTS_JoC_refined.pdf | 554946 | `5876b0bea5c6dbf8bae4558b823e23b45945cc48cc3eb39f12aabfbd97eff8a4` |
| P4_Pareto_MCTS_JoC_SM.tex | 14919 | `97cd87cd0356e655771a9304d95d02ae3ff56185eec7c07e480657402de805d1` |
| P4_Pareto_MCTS_JoC_SM.pdf | 315242 | `f862fe2fb921e4ca6d806f57fc5ff16d8af118950eaf2eb4347fcceebef49301` |
| Cover_Letter_P4_JoC.tex | 3636 | `903f896b53a6a10fd9aa43050babf36e1116cb2bcc2e8b2ed4796146f2efc495` |
| Cover_Letter_P4_JoC.pdf | 77049 | `e5b5fa182c92479598f16475358dc435657960fc84d68b2609a5e439e527acca` |
| P4_Bibliography.bib | 14444 | `8cf52b66434c9db55b1e5fc7698aa7d11d1db6258d3df535bad24ed56b86ef1e` |

## Graphics

| File | Size (B) | SHA-256 |
|---|---:|---|
| benchmark_efficiency.pdf | 18416 | `9ff4c7297f5b778eae7987dc4a74b865041e0648b1943fa60dc3f683a5b02444` |
| benchmark_efficiency.png | 154890 | `3a0e1b4b15dfc2a922e89e903df3db19d69e9f5204b9fd04f4a77704c2d071fc` |
| benchmark_reward_bar.pdf | 22013 | `1c96a7692529830337267ee16494d1f6587b8b7e6e0e97e077c1660800369499` |
| benchmark_reward_bar.png | 178734 | `d6c24dfd74751781eaab800d3eb93b5abda80218c60289493daee7467201c438` |
| Graphical_Abstract_P4.png | 73582 | `d452cad43196ab837b98591405754687a082425450fd6ca4bb1b8f9cc775b100` |
| p4_evidence_overview.pdf | 25042 | `30ebb88ecd0bd3d39aa7f4dbfe13a05e9564887f0843a9119d091124e5ae9ff3` |
| p4_evidence_overview.png | 266068 | `2d1e6c29b2ad9bb9c1acc7d826f45ee614794051296ea91216bc2b6e71f6d6fe` |
| p4_hv_vs_ref.png *(unused — cited in neither document; see provenance)* | 78247 | `4dabf1792a1ae3cd02de6e0c524ee1aca608557bcafd6f0e946ece297cf1e560` |
| pareto_front.pdf | 21592 | `68f9d32051b2fe06668ce76e11bddadf1111f8113590e6662da72f2016d47d66` |
| pareto_front.png | 169386 | `80a9c0b8fd7c109ea8c3cfa0a9aff26ff18bba5fd06bd180c0d1b66a79ed26b1` |
| pareto_front.sidecar.json | 709 | `6c3ad25ad69e28e785ce9f1cd8f36d9ca3086b9b8fb2524a23b5950e68da8f02` |
| rrs_pns_profile.pdf | 38165 | `6306b199cfaeaa79b2b07d928be56ad0efc7ff34c4e17d7a45bd3597184e362e` |
| rrs_pns_profile.png | 239441 | `b42e57e5da6d4394048651afd10b29c59518f8f34df6cb3bdfe33f3b2e12f1dd` |
| scaffold_diversity.pdf | 16661 | `da89e0ba25d89da8e8a01f11edb62863154570c9a176b40ad6616347d48dd264` |
| scaffold_diversity.png | 269940 | `58f245b203e67b63aed74a1d5e87bef1e2bb9efb382cec1c907f9ad753dcdf68` |

## Provenance notes

- All six data figures were generated by `scripts/p4_replot_publication.py` from the deposited result files — first pass 2026-08-15, re-rendered on 2026-08-17 (layout fixes, then the footer-band residual, then the reproducibility re-run recorded below): Okabe-Ito colour-blind-safe palette with redundant marker encoding, per-seed observations shown, 600 dpi opaque PNG plus vector PDF sidecar, Type 42 fonts. No value was recomputed, filtered or rescaled. The graphical abstract was deliberately not regenerated.
- The bibliography is produced by BibTeX, not manual `\bibitem` entries: `P4_Bibliography.bib` holds 36 entries, of which 20 are cited. The count is confirmed two independent ways — 20 `\bibitem` lines in the generated `.bbl` and 20 unique keys across all `\cite` commands in `refined.tex` — with 0 undefined-citation warnings in the log. The 16 uncited entries are retained as a working pool and reach no output file.
- Captions for Figures 1 and 3 were updated to match the new content (per-seed points; logarithmic time axis). All other captions are unchanged.
- Fragment vocabulary standardised on the v12 set: full = 80 fragments, medium = 37. The SM vocabulary ablation retains the earlier development vocabulary and is labelled as such in its caption.
- Table 4 (`tab:allfrag`, job 15133) is confirmed cell-for-cell by section 1.12 of the long-form P4 DAR. Its per-seed output files are not in the repository deposit; the Data availability section states this explicitly.
- The Abstract, Limitations and Conclusions were calibrated against the ledger in eight edits, each traced to a named artifact and logged in `project-tracking.md`. Every number in the Abstract and Conclusions traces to `tab:sm_benchmark`, `tab:sm_pareto` or the long-form DAR; no number was introduced that is not already stated upstream.
- 2026-08-17 — a from-primary-source recomputation of the reported numbers was run directly against the deposited CSV/JSON artifacts. Verified reproduced: the factorial-ablation marginal means and all five $\Delta$ of `tab:sm_component_ablation` (recomputed from the 160 `Reward` values in `results/ablation/p4_ablation_config_{0..31}.csv`); the four vocabulary means and SDs of `tab:sm_vocabulary_ablation` and its ANOVA F = 350.1 (df 3/36) and Tukey medium−minimal $p_{\mathrm{adj}}$ (recomputed from the 40 per-seed files); the v12 and full reward weight vectors, both summing to 1.000; `tab:sm_diversity` cell-for-cell; the RF activity-oracle panel figures; and `tab:pareto`/`tab:sm_pareto` against `results/pareto/merged_pareto_front.csv`.
- 2026-08-17 — one description defect found and fixed. The hypervolume 1.2366 is computed over **four** active objectives (MPO, SYBA, RRS, PNS), not three: `merged_pareto_front_recompute.log` lists all four in `active_objectives`, `syba` is non-constant in the merged front so `_detect_active_objectives()` retains it, only `sa` (constant 3.0) is dropped, and a genuine three-objective recompute gives 1.1244 ≠ 1.2366 (the subset is identified and the figure re-derived independently in the 2026-08-18 entry below; cite it as **1.124376**, not 1.1244). The value was correct; the count and the bound were not. Corrected in five places — `refined.tex` abstract, `tab:front_comp` caption (which also cross-referenced the wrong section) and the Pareto-front-maintenance methods paragraph; `SM.tex` S1 SYBA paragraph and S2 hypervolume paragraph — so the bound reads $1.1^{4}=1.4641$ for the front-internal convention and 19.45 for the cross-method convention. No numeric result changed. `scripts/p4_pareto_provenance_check.py` remains green: it gates on the value 1.2366 and on `sa` being inactive, not on the objective count or the bound, so the corrected wording does not affect it. (It carries no `assert` statements — the checks run through a `check(cond, label, detail)` helper that prints PASS/FAIL, collects failures and exits nonzero, at `:143` for the recomputed hypervolume, `:171` for the recompute-log value and `:146` for `sa` being excluded as constant.)
- 2026-08-17 — the selection-ablation provenance gap is closed, and an earlier reading of it withdrawn. The hypervolumes of `SM.tex` S5 (15.56 scalar-proxy, 15.49 Pareto-restricted) are recorded in `P4_NOVELTY_EXPERIMENTS.md` §N2 with their merged front sizes (60 and 54 after deduplication) and per-seed front sizes ([30, 31, 66, 19, 39] and [34, 32, 40, 46, 18]), and in the long-form DAR at l. 498. The stated budget is confirmed by `scripts/p4_novelty_n2.sbatch`: a 5-seed array (`--array=0-4%5`) with `N_ITERATIONS` defaulting to 700 and passed explicitly as 700 in both documented invocations, medium vocabulary, $c_{\mathrm{PUCT}}=5.0$, policy temperature 0.8, benzene root and 10 steps — the same configuration as S1. The "neither rule showed a measurable advantage" wording is verifiable: the per-seed $\Delta$HV vector recorded in §N2 is the same vector frozen into the self-test of `scripts/p4_n2_power_test.py`, and its statistics were reproduced by hand — mean $+0.531$, SD $1.402$, paired $t=0.85$ (not significant), minimal detectable effect 1.76 at n = 5 and 0.88 at n = 20, ratio 0.61 — so the effect lies below the minimal detectable difference even at the larger sample.
- 2026-08-17 — withdrawn: the "convention-dependent conclusion" flag recorded earlier the same day. It compared 15.56/15.49 against the 1.0628 and 0.6380 stored in the `hypervolume` column of `results/pareto/n2_proxy_merged.csv` and `n2_pareto_merged.csv` and read the difference as a 66 %-versus-0.4 % flip. Those columns are the per-run search-time front-internal indicator, not a second measurement of the cross-method quantity, so the two are not alternative values of the same number and no flip exists. What remains is a provenance tier, not a discrepancy: 15.56/15.49 are ledger-recorded rather than stored in a CSV, so reproducing them requires re-running `p4_merge_pareto_fronts.py` under the shared normalisation. This is one tier weaker than `tab:front_comp`, whose five rows were verified exactly against `results/pareto/p4_multiobj_front_summary.csv` — HV 13.845/16.589/15.488/15.523/18.988 with the C-metric, IGD, IGD_loo and spread columns all matching at the displayed precision. The 15.49 shared by GA's front and Pareto-restricted selection is a rounding coincidence, as the ledger states: GA's exact value is 15.488134565469615.
- 2026-08-17 — the figures were inspected at final printed size: all seven files under `Graphics/`, each checked against the table or ledger entry it derives from. Every numeric element matches `tab:sm_pareto`, `tab:sm_benchmark`, `tab:sm_diversity` or `tab:sm_component_ablation`, and no figure contradicts a table. Four rendering defects were found, all in the output of `scripts/p4_replot_publication.py` and none of them altering a reported value: x tick labels collided in **both** panels of `rrs_pns_profile`, printing `MCTS+ScafVAE` and `Genetic algorithm` as one string; the `scaffold_diversity` legend was drawn inside the data region over the markers, with the provenance footer overprinting its fourth entry; the P1 candidate (MPO 0.910, SYBA 1.000) was plotted but unlabelled in panel B of `p4_evidence_overview` while P2, P3 and P4 were labelled, inconsistent with `pareto_front.png`; and in `benchmark_efficiency` the Greedy and GA points fell left of the first labelled logarithmic tick with no minor-tick labels, so their wall-clock values could not be read off the axis.
- `p4_hv_vs_ref.png` is listed in the Graphics table above but is **not part of the submission set**: it is cited in neither `.tex`, uses the default matplotlib palette rather than Okabe-Ito, carries no provenance footer, has no `.pdf` sidecar and has a title clipped to "…to reference". The replot note above covers the six data figures, not this one. Its content is correct — it reproduces the reference-point sweep of `P4_NOVELTY_EXPERIMENTS.md` §N3, hypervolume rising from 15.2 to 37.8 as the reference point moves from 1.00 to 1.50 — so the file is kept and hashed for the record, and its row is labelled unused rather than deleted. Dropping it from the deposit is an author's decision.
- 2026-08-17 — all four rendering defects were fixed in the source of `scripts/p4_replot_publication.py` and the script was then re-run, rewriting all twelve PNG/PDF files of the six data figures. Each fix is layout-only, and none touches a plotted value, an axis range, a statistic or a caption: the method names in both panels of `rrs_pns_profile` are wrapped over two lines through a new `LABEL_WRAP` mapping; the `scaffold_diversity` legend is moved above the axes in two columns and its figure height raised from 92 mm to 100 mm, with an opaque background added to the provenance footer; `_pareto_axes` gains a per-candidate `LAB_OFFSET` table that pushes the P1 label leftwards and draws all four labels above the markers, plus a 10 % x margin; and `benchmark_efficiency` gets explicit x ticks at 2, 5, 10, 20, 50 and 100 s with the unlabelled minor ticks suppressed. One cosmetic item was fixed at the same time: the panel-A mean annotation of `p4_evidence_overview` printed its negative sign with binary-operator spacing ("mean = − 0.0075"), so a `_mnum()` helper now braces the sign. The `p4_hv_vs_ref.png` palette is untouched — that figure is not produced by this script, and `Graphical_Abstract_P4.png` and `pareto_front.sidecar.json` were likewise not rewritten.
- 2026-08-17 — the re-rendered files were then inspected at final printed size and all four defects are confirmed gone: both panels of `rrs_pns_profile` print the method names over two lines with the `7/20`, `12/19`, `11/19`, `1/1` annotations unchanged; the `scaffold_diversity` legend sits above the axes in two columns, the raster height rising from 2173 to 2362 px, which independently confirms the 92 mm to 100 mm change; panel B of `p4_evidence_overview` labels all four candidates and its mean annotation now reads "mean −0.0075, 95% CI [−0.0107, −0.0043]" with an ordinary unary minus; and `benchmark_efficiency` carries labelled ticks at 2, 5, 10, 20, 50 and 100 s, so the Greedy (1.9 s) and GA (2.0 s) positions can be read off the axis. Every plotted value still matches its source table, so all captions remain valid, and the script's own consistency printout reproduces `tab:sm_benchmark` exactly — Random 0.6724 ± 0.0056 at 50.4 s, MCTS+ScafVAE 0.6649 ± 0.0068 at 92.1 s, Genetic algorithm 0.6453 ± 0.0124 at 2.0 s, Greedy 0.4278 ± 0.0000 at 1.9 s. Two residuals were logged. First, lifting the `scaffold_diversity` legend left the provenance footer's opaque background masking the lowest MCTS marker at data (−0.06, −0.685); the fix reserves a blank band by extending the lower y limit by 14 % of the data range, which adds whitespace only — no coordinate is rescaled and the caption is unaffected. Second, in `benchmark_efficiency` the `MCTS+ScafVAE` annotation sits tight against the right spine; this is cosmetic and left as an author's decision.
- 2026-08-17 — **O1 closed.** The footer-band fix had been written into the replot source at 13:39 but the figures on disk still dated from 13:21, and both PDFs had been built at 13:52 from those stale figures, so the manifest could not describe the working tree. The cycle was completed in one pass: `p4_replot_publication.py` re-run (`exit=0`, all twelve PNG/PDF files rewritten, consistency printout again reproducing `tab:sm_benchmark` exactly), then `latexmk -pdf -interaction=nonstopmode -halt-on-error` on both documents (both `exit=0`, stderr empty, 0 errors / 0 undefined / 0 overfull / 0 underfull / 0 rerun-requested), then this file regenerated as a whole — sizes, hashes, page counts and timestamp read in a single pass rather than patched row by row. `scaffold_diversity.png` settled at 269940 B (2716 × 2362 px) and its `.pdf` at 16661 B; `refined.pdf` at 554945 B / 14 pp. and `SM.pdf` at 315239 B / 4 pp. Two figure sizes forecast before this pass (267128 B and 315206 B) were superseded by it, since the footer-band change re-rendered the affected file again.
- 2026-08-17 21:26 UTC — the whole cycle was run a second time on author instruction, and it doubles as a **figure reproducibility check**. All three commands again returned `exit=0` with empty stderr and the same clean log counts, and the consistency printout again reproduced `tab:sm_benchmark` exactly. All six data-figure **PNGs are byte-identical to the previous run** — same sizes and same SHA-256, `scaffold_diversity.png` again 269940 B at 2716 × 2362 px and 600 dpi — so the rasters that reach the page are deterministic given the deposited inputs, and no plotted value, axis range or annotation moved. What did change is confined to embedded creation timestamps: the six figure `.pdf` sidecars keep their byte sizes exactly (18416, 22013, 25042, 21592, 38165, 16661) but take new hashes, and the two documents move by a few bytes as the date string is re-encoded — `refined.pdf` 554945 → 554946 B and `SM.pdf` 315239 → 315242 B, both with new hashes and unchanged page counts (14 and 4). The rows above are the current build; the sizes and hashes quoted in the O1 note are the previous one, kept for the record. `Graphical_Abstract_P4.png`, `p4_hv_vs_ref.png`, `pareto_front.sidecar.json`, the three `.tex` files, the `.bib` and the cover-letter PDF are untouched, and their hashes are unchanged across both passes.
- 2026-08-17 — **the maker≠checker gate has now run, and it did not pass.** On author instruction ("launch a read-only agent with fresh context on the claim-to-ledger mapping") an independent checker — separate agent, fresh context, read-only tools — reviewed the claim-to-ledger mapping. This session then re-verified every finding against primary source under step 5.2, *trying to reject the checker*. Verdict: **No — qualified-negative.** The gate is closed in the sense that it ran; it is not satisfied. Three CRITICAL defects were **upheld**, each re-derived by command: Greedy is scored under a different rule from every method it is compared to (`scripts/p4_mcts_benchmark.py:179` returns `oracle(state)` where Random at :147 and GA at :215 return `best_reward`, while `refined.tex:318` states the opposite rule and `:282` claims that correction as contribution (iii)); the fragment priors are heuristic by their own source comment at `scripts/p4_mcts_policy.py:33` yet described as ChEMBL27-derived at `refined.tex:313`; and `scripts/p4_benchmark_multiobj.py:27` reads the v11 benchmark directory although the v12 directory exists with 20 seed CSVs of its own. A fourth CRITICAL was **withdrawn on re-verification**, which is why the count fell from four to three: the alleged activity-panel substitution does not exist. The two file names are two different panels, and each document cites the one its own pipeline reads — `Project5.../results/p5_public_chembl_malaria.csv` holds 22 447 rows of which 19 321 are active, matching `SM.tex:97` and the three scripts that open it, while `p5_canonical_panel.csv` holds 19 836 rows of which 14 721 are active, matching `refined.tex:288`, `scripts/p4_activity_rf_oracle.py:24` and `results/pareto/p4_activity_rf_oracle.json`. Every row count and active count is correct on both sides. What survives is a disclosure gap re-graded to MEDIUM: the RF training panel is 74.2 % active and that class prevalence is stated nowhere, so AUC 0.9479 ± 0.0040 is reported without the base rate a reader needs to weigh it. Six HIGH were upheld: benzene-versus-methane root between the two sbatch configurations (`p4_pareto_array.sbatch:63` versus `p4_benchmark_molecules_opt_v12_array.sbatch:70`, both documents saying only "rooted at benzene"); three of five "retained" hyperparameters never actually screened — the manuscript's own "32 configurations" identifies the screen as `QUICK_GRID`, $2^{5}=32$ cells, and $pw_{\alpha}=0.5$, $pw_{k}=1.0$, $T=0.8$ appear in none of its level sets, each being instead the runner's own default, which is also why the printed marginals are identical to four decimal places (a marginal over a factor held constant is the grand mean); the retained virtual loss $\nu=0.01$ at `refined.tex:103` against a runner default of 0.05, with nothing on disk stating which of the two produced the reported benchmark — a defect **neither checker reported**, surfaced while verifying DC4; Table 4 with no per-seed artifact; an environment file that does not exist; and an untraced 30-iteration screening budget against a script default of 50. One checker HIGH was **rejected** — the progressive-widening range it alleged is in neither document, and `refined.tex:313` matches `scripts/p4_mcts_agent.py:381` exactly — and one was **re-graded to MEDIUM**, the 2.6× rollout factor, which traces but is not isolated by a controlled ablation. DC1 ran: 119 numeric values, 109 TRACED, 6 MISMATCH, 4 UNTRACED; its row-level inventory was produced in the checker's context and **never written to disk**, so only the counts survive and the detail is regenerable only by re-running DC1 — it must not be reconstructed from memory. DC2 **PASSED**: every Abstract and Conclusions figure is contained in the table it cites, with the negative signs on $\Delta$ and both confidence bounds carried correctly. DC3 **PASSED**: the two hypervolume conventions are consistent and declared non-comparable in all three places they meet — the front-internal figure 1.2366 against the $1.1^{4}=1.4641$ bound on four active objectives, and the cross-method figure against $(1.1+1)^{4}=19.45$ after per-method normalisation and post-normalisation sign flip. No defect above LOW; the checker's one suggestion is an addition, not a weakening — extend `scripts/p4_pareto_provenance_check.py` to assert `active == ["mpo","syba","rrs","pns"]` so a future objective-set change cannot pass silently. DC4 **PASSED on integrity, FAILED on completeness**: the negative result is stated without softening and with no sign or bound error at every one of the five places `refined.tex` carries a signed effect (lines 63, 138, 248, 272, 342), and `SM.tex` carries no inferential statistic at all, so there is nowhere for it to be contradicted; but the two-paragraph Limitations at `refined.tex:284`--288 covers only fragment vocabulary and computational proxies, omitting six items disclosable today, of which the seventh — Table 4's missing artifact — is already disclosed at `:358`. Full record: `outputs/critical-reviews/review-claim-ledger-20260817.md`. **The central negative result is untouched by every finding:** Pareto-MCTS still does not beat Random on scalar reward (paired $t_{19}=-4.97$, $p=0.000085$, $\Delta=-0.0075$, 95 % CI $[-0.0107,-0.0043]$). Packaging is therefore no longer blocked by an unrun gate — all four done-criteria are closed — but by three CRITICAL author decisions, the first of which changes a published number wherever 0.4278 appears (`refined.tex:63, 72, 113, 125, 138, 224, 232, 278` and `SM.tex:214`, plus 0.4593 at `:260`) and deletes or rewrites `refined.tex:276`--278, whose mechanism story rests on the residue of the very bug the paper claims to have fixed.
- 2026-08-18 — **the last unverified value of that audit is closed, by independent re-derivation.** The three-objective hypervolume 1.1244, written into this file and into `project-tracking.md` on 2026-08-17 and quoted again by the checker, had never been recomputed by anything other than the routine that produced the number it was contrasted with. `scripts/p4_hv_independent_recheck.py` now re-derives it from the specification alone: it re-implements the documented algorithm of `scripts/p4_mcts_pareto.py:168`--198 (drop constant objectives, negate maximised ones, min--max normalise each surviving column, reference point 1.1 per active dimension) and evaluates the hypervolume by exact inclusion--exclusion over the 15 nonempty subsets of the four front points, $\mathrm{HV}=\sum_{S\neq\emptyset}(-1)^{|S|+1}\prod_{d}\max(0,\,r_d-\max_{p\in S}p_d)$, which is closed-form at n = 4 and needs no dimension sweep. It imports nothing from `p4_mcts_pareto`, so it cannot inherit an error in the method it checks. The gate was satisfied before any three-objective figure was used: the four-objective value comes out **1.236644** against the published 1.2366, the active set is confirmed as `['mpo', 'syba', 'rrs', 'pns']` with only `sa` dropped as constant, and the exact routine agrees with `pymoo` to 1e-9. The three-objective sweep then resolves the figure to its subset — **dropping `syba` gives 1.124376**, the quoted 1.1244 exactly. One caution the 4-decimal form hid: dropping `pns` instead gives **1.124289**, a different subset that agrees to four decimals and differs only at the fifth, so "1.1244" does not identify which objective was omitted and the value should be cited at six digits wherever it appears; the script reports the two cases distinctly rather than declaring a match twice. The 2026-08-17 correction is unaffected — `syba` is non-constant on this front, so *any* three-objective recompute is the wrong computation regardless of which objective it drops. Separately, one LOW item of the audit was **withdrawn** the same day: it alleged stale three-objective text at `outputs/analysis/number-reconciliation-20260815.md:81` and `:85`, but `grep -n "three-objective\|three objective\|3-objective\|1\.1244"` returns nothing across all 143 lines of that file, and the block at `:81`--91 in fact states the four-objective case and the 1.4641 bound. No manuscript number changed; the two `.tex` sources were not touched.
